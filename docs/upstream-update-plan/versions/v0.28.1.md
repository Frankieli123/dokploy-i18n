# v0.28.1

## 状态
- 优先级：中
- 状态：已审查 / 已吸收
- 对应范围：`v0.28.0 -> v0.28.1`

## 上游来源
- Release: https://github.com/Dokploy/dokploy/releases/tag/v0.28.1
- Compare: https://github.com/Dokploy/dokploy/compare/v0.28.0...v0.28.1

## 上游实际变更
这版很小，核心只有 4 个提交：

1. `@dokploy/trpc-openapi` 升到 `0.0.17`
2. `settings.getOpenApiDocument` 的 `baseUrl/docsUrl` 路径修正
3. `mariadb/mysql/redis` 更新弹窗增加 `isOpen` 状态
4. 上述 3 个弹窗的格式整理

## 本地审查结论

### 已覆盖
#### 1. OpenAPI / trpc-openapi
本地已经完成更深一层适配：
- 已使用 `@dokploy/trpc-openapi 0.0.17`
- `settings.getOpenApiDocument` 路径已对齐到 `/api`
- 本地还额外做了 zod v4 loader + schema sanitize 兼容
- 当前 `pnpm --filter=dokploy run generate:openapi` 已通过

结论：
- 上游这部分 **已被本地更完整方案覆盖**
- 无需再单独吸收

### 已吸收
#### 2. MariaDB / MySQL / Redis 更新弹窗状态管理
已落地：
- `apps/dokploy/components/dashboard/mariadb/update-mariadb.tsx`
- `apps/dokploy/components/dashboard/mysql/update-mysql.tsx`
- `apps/dokploy/components/dashboard/redis/update-redis.tsx`

吸收内容：
- 增加 `isOpen` 状态
- 更新成功后自动关闭弹窗

结论：
- 这是低风险 UX 优化
- 已直接吸收

## 冲突评估
- 不会删除或削弱本地新增功能
- 不影响 AI 面板
- 不影响 Git 独有设置
- 不影响备份增强
- 不涉及高冲突数据模型

## 验证
已通过：
- `pnpm --filter=server run typecheck`
- `pnpm --filter=dokploy run typecheck`
- `pnpm --filter=dokploy run generate:openapi`

## 最终结论
`v0.28.1` 属于小版本整理。

对当前分叉而言：
- OpenAPI 部分：**本地已超前覆盖**
- 弹窗状态部分：**已吸收**

因此可视为：
### `v0.28.1` 已完成，无额外待处理项
