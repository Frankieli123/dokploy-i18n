# v0.27.1

## 状态

- 优先级：高
- 状态：已完成
- 对应范围：`v0.27.0 -> v0.27.1`

## 上游来源

- Release：https://github.com/Dokploy/dokploy/releases/tag/v0.27.1
- Compare：https://github.com/Dokploy/dokploy/compare/v0.27.0...v0.27.1

## 最终结论

`v0.27.1` 已完成一轮“按我们分叉实际情况裁剪”的吸收。

本次处理原则：

1. 不删除我们现有新增功能
2. 不接受上游去 i18n 方向的改动
3. 只吸收安全、稳定性、通用可用性修复
4. 对高冲突功能保持暂缓

## 本次已实施

### 1. docker-container-logs 参数校验

- 文件：
  - `apps/dokploy/server/wss/docker-container-logs.ts`
  - `apps/dokploy/server/wss/utils.ts`
  - `packages/server/src/wss/utils.ts`
- 内容：
  - 增加 `tail / since / search` 白名单校验
  - 默认值前置归一化
- 结果：
  - 已实施

### 2. ZIP drop 安全防护

- 文件：
  - `packages/server/src/utils/builders/drop.ts`
- 内容：
  - 增加危险节点拒绝
  - 增加路径逃逸校验
- 说明：
  - 实施时没有照抄上游 `BASE_PATH` 级别判断
  - 改成了**以当前 outputPath 为边界**的更严格校验
- 结果：
  - 已实施

### 3. environment 删除前服务检查

- 文件：
  - `packages/server/src/services/environment.ts`
- 内容：
  - 删除环境前检查 applications / compose / mariadb / mongo / mysql / postgres / redis
- 结果：
  - 已实施

### 4. compose stack 删除修复

- 文件：
  - `packages/server/src/services/compose.ts`
- 内容：
  - 去掉对 `cd ${projectPath}` 的强依赖
  - 去掉本地多余重复 `execAsync`
- 结果：
  - 已实施

### 5. Traefik IDN / punycode

- 文件：
  - `packages/server/src/utils/traefik/domain.ts`
- 内容：
  - 非 ASCII host 生成 router rule 时转 punycode
- 结果：
  - 已实施

### 6. compose domain 错误提示增强

- 文件：
  - `packages/server/src/utils/docker/domain.ts`
- 内容：
  - 缺失 serviceName / 服务不存在时给出更清晰错误
- 结果：
  - 已实施

### 7. update-server releaseTag 条件显示

- 文件：
  - `apps/dokploy/components/dashboard/settings/web-server/update-server.tsx`
- 内容：
  - 仅当 `releaseTag === "canary" || releaseTag === "feature"` 时显示 tag
  - 保留现有 i18n 和 UI 结构
- 结果：
  - 已实施

## 本次确认“已被本地覆盖，无需实施”

### 1. cluster replicas 转 number

- 上游原路径：
  - `apps/dokploy/components/dashboard/application/advanced/cluster/swarm-forms/mode-form.tsx`
- 本地实际情况：
  - 你们当前分叉没有这个路径
  - 现有实现位于：
    - `apps/dokploy/components/dashboard/application/advanced/cluster/show-cluster-settings.tsx`
- 结论：
  - 当前表单 `onChange` 已显式做 `Number(value)`
  - 该项已被本地结构覆盖
  - 无需额外改动

### 2. queue / worker 保护

- 文件：
  - `apps/dokploy/server/queues/deployments-queue.ts`
  - `apps/dokploy/server/queues/queueSetup.ts`
- 结论：
  - 本地已有更强实现
  - 无需回抄 `v0.27.1`

### 3. callback URL 移除

- 文件：
  - `apps/dokploy/pages/index.tsx`
- 结论：
  - 当前本地社交登录调用中不存在该问题
  - 无需处理

## 本次明确跳过

### 1. 去 i18n / 英文硬编码回退

- 跳过原因：
  - 与项目方向正面冲突
  - 违反“不删除我们新增功能”的原则

### 2. proprietary SSO / trusted origins / license 相关

- 跳过原因：
  - 与当前本地 panel domains / trusted origins 方案不一致
  - 收益不高，冲突偏大

### 3. Teams / Soft Serve / deployment 清理 UI

- 结论：
  - 暂缓
- 原因：
  - 不是当前高价值通用修复
  - 冲突或改造成本更高

## 验证结果

### 已通过

1. `pnpm --filter=server run typecheck`
   - 通过

2. `pnpm --filter=dokploy exec vitest --config __test__/vitest.config.ts run __test__/wss/utils.test.ts __test__/traefik/traefik.test.ts __test__/drop/drop-security.test.ts --reporter basic`
   - 通过
   - 结果：`3 passed / 17 tests passed`

### 额外说明

`pnpm --filter=dokploy run typecheck` 仍失败，但失败点在：

- `apps/dokploy/server/api/routers/user.ts`

报错是现存的 `additionalHosts` 类型不匹配，和本次 `v0.27.1` 吸收改动无关。

## 本次新增测试

- `apps/dokploy/__test__/wss/utils.test.ts`
- `apps/dokploy/__test__/drop/drop-security.test.ts`
- `apps/dokploy/__test__/traefik/traefik.test.ts` 新增 punycode 断言

## 实施记录

- [x] 完成上游比对
- [x] 完成后端高优先级修复
- [x] 完成前端实际需要的最小修复
- [x] 确认 cluster replicas 项已被本地覆盖
- [x] 完成针对性测试验证
- [x] 记录跳过项

## 对下一版本的提示

进入 `v0.28.0` 时应继续保持相同原则：

1. 不整体跟大版本
2. 拆分吸收高价值小块
3. Node 24 / pricing / patches 之类先单独评估，不默认接
